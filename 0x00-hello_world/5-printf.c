#include <stdio.h>
/**
 * main - Using the print command and ending with a new line
 * Return: Always (0) (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @r: the int to be checked
 * Return: absolite value
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	return (-r);
}
